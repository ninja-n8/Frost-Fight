using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KA_SpawningPlatform : MonoBehaviour {

	/*public GameObject SpawnPlatform;

	public float spawnplat;

	public float platformDelay;

	// Use this for initialization
	void Start () {
		StartCoroutine(spawnPlatform());

		SpawnPlatform.SetActive = false;
	}
	
	// Update is called once per frame
	IEnumerator spawnPlatform () {
		if (SpawnPlatform == false) {
			yield return new WaitForSeconds (spawnplat);

			SpawnPlatform.SetActive = true;
		} 
		else if (SpawnPlatform == true) {
			yield return new WaitForSeconds (platformDelay);

			SpawnPlatform.SetActive = false; 
		}
	}*/
}
